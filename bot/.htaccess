# Protect configuration and session files from web access
<Files "config.php">
    Order allow,deny
    Deny from all
</Files>

<FilesMatch "\.(txt|log)$">
    Order allow,deny
    Deny from all
</FilesMatch>

# Prevent directory listing
Options -Indexes
